# Sign a payload by hashing it together with a secret key.
# Keys input: {"keypair": {"private_key": "base64"}}
# Data input: {"payload": "string to sign"}
# Output: {"hash": "base64"} — this hash is the signature
Scenario 'hash': sign

Given I have a 'string' named 'private_key' in 'keypair'
and I have a 'string' named 'payload'

When I append 'private_key' to 'payload'
When I create the hash of 'payload'

Then print the 'hash'
